# 《浮生仙途》玩法与平衡设计（诸天万界版）

> 设计定位：AI 主持 + 玩家抉择驱动的「诸天万界文字修仙 Roguelike」。
> 一句话平衡哲学：**自由叙事交给 AI，节奏与生死交给规则。** 因为数值是 AI 自由填写的，所以"平衡"的本质不是去调一个个魔法常数，而是**给 AI 戴上数值笼头（delta 上下限）+ 把随机性收归确定性规则**，让天马行空的剧情不破坏核心节奏。

---

## 一、设计目标与取舍

- **爽点**：抉择有重量、突破有顿悟、世界会真死、重投有新篇。
- **留存钩子**：前 30 程内起伏密集、钩子不断；境界跃迁即篇章节点；神魂册跨局累积"因果力"。
- **明确不做的平衡**：不堆数值膨胀（当前 `maxQi` 到飞升期已经是 `Infinity`，必须封顶）；不靠抽卡；不做 P2W；不因灵力值高低而让世界"精彩程度"不同（这是你拍板的设定）。

---

## 二、核心循环（单局）

```
探索(地域/宗门/秘境) → 抉择(3-4 选项·带风险标签)
   → 历练(AI 叙事 + 数值增量 delta)
   → 结算(delta 应用 + 神魔大战危机判定)
   → 突破(境界跃迁 / 反噬重伤)
   → 飞升离界  或  此界湮灭(重投新界)
```

要点：**每一次抉择都带风险标签**（`[平安] / [凶险] / [致命]`），且标签必须按玩家当前境界判定——低境多出 `[致命]/[凶险]`，高境多出 `[凶险]/[平安]`，让"同样的胆大妄为"在不同阶段有不同分量。

---

## 三、元循环（诸天万界）

- 一道**投影 = 一局**。灵力值（3–9）只决定本界**境界天花板**与**时间压力**，不决定内容质量。
- 神魔大战随时可能湮灭此界（仅当 `realmLevel < realmCapLevel` 时），湮灭 → 旧界入神魂册·已陨落 → 自动重投新界（元循环）。
- 已陨落世界**永久**留在神魂册；只有修到「万界神魔」之姿（触及 cap 并镇守此界）才能真正"复活"/稳住旧界。
- 灵力值低的界（如 cap=4 元婴）：窗口短、压迫强，但体验密度高；灵力值高的界（cap=10）：近乎无限时间，适合慢慢铺陈大剧情。

---

## 四、境界与突破 —— 当前最大失衡点（P0）

### 现状问题
`handleBreakthrough()` 里：
```js
const diff = nextRealm.breakthroughDiff;            // 0.2(炼气→筑基) → 0.99(大乘→飞升)
const successRate = Math.min(0.95, diff + (c.comprehension - 6) * 0.02);
```
`breakthroughDiff` 随境界**递增**（炼气 0.2 → 大乘 0.99），而它又被直接当成了成功率。
→ 结果：**第一层突破只有 20% 成功率，最后一层 99%。完全倒置**——新手最挫败，老手最无脑。这是全游戏最该先修的一处。

### 修正模型
```
突破成功率 = 基础易度(随境界递减) + 悟性修正 + 丹药修正 − 风险修正
总成功率 clamp(0.05, 0.95)
```

| 突破 | 基础易度 | 说明 |
|---|---|---|
| 炼气 → 筑基 | 0.85 | 新人友好，先给正反馈 |
| 筑基 → 金丹 | 0.72 | |
| 金丹 → 元婴 | 0.60 | |
| 元婴 → 化神 | 0.50 | |
| 化神 → 炼虚 | 0.42 | |
| 炼虚 → 合体 | 0.35 | |
| 合体 → 渡劫 | 0.28 | 天劫凶险 |
| 渡劫 → 大乘 | 0.22 | |
| 大乘 → 飞升 | 0.15 | 半步飞升，万里挑一 |

- 悟性修正：`(悟性 − 6) × 0.02`，封顶 +0.15。
- 丹药修正：`破障丹 +0.15`（与洗髓丹的悟性增益叠加）。
- 风险修正：以 `[致命]` 方式强冲 `+0.10`（赌命），但失败惩罚更重。

### realm_progress 必须"服务端钳制"（P0 配套）
当前 AI 能一次性 `return { realm_progress: +100 }` 直接突破，或 `+0` 让玩家永远卡住——进度快慢完全看 AI 心情。
规则（在 `processAction` 应用 delta 前 clamp）：
- 单回合 `realm_progress` 增量钳制 **[−5, +12]**。
- 基础 +3~+6；在"匹配境界"的险地历练 / 选"修炼"类抉择额外 +2~+6。
- 风险标签映射：`[平安] +2`，`[凶险] +4~+6`，`[致命] +6~+12`（但伴随 HP 风险）。
- 突破失败回退到 `max(0, progress − 40)`、HP ×0.5（保留现有手感）。
- 这样**一界正常需 25~60 回合摸到天花板**，正好和危机曲线对齐。

---

## 五、属性与经济平衡

### 属性用途表
| 属性 | 作用 | 平衡要点 |
|---|---|---|
| 灵力 qi | 施法/突破资源，突破后回满 | 不超过 `maxQi` |
| 气血 hp | 战斗与事件，归零=死（除非有续命物） | 单回合增量钳制（见六） |
| 寿元 lifespan | 每 11 回合 −1；突破 ×1.5 上限并回满 | 持续突破=实质永生（符合设定），停滞才老死 |
| 声望 reputation | 开宗立派 / 接高阶任务门槛 | 慢积累，防速成 |
| 正义 / 邪恶 | NPC 态度（≥60 生死之交 / ≤−60 敌对） | 加门槛机制（见七） |
| 悟性 | 突破率、学技速度 | 出身差异的主要补偿项 |
| 体质 | HP 上限与抗打断 | 妖族血脉 1.5 倍，作为体验差异 |
| 灵石 | 唯一硬通货 | 经济锚点（见下） |

### 灵石经济锚点
- **收入**：日常任务 +20~80/程；探索奇遇 +50~300；卖货/打劫波动大；高境任务指数增长。
- **支出**：聚气丹 10、培元丹 15、洗髓丹 200、破障丹 500、回春丹 300、凝神丹 1000、九转还魂丹 100000。
- **设计意图**：低境"攒钱买破障丹冲关"、中境"卖宝换资源"、高境"灵石只是数字"。**破障丹是核心消费锚**，必须稳定可得。

### 出身 / 灵根多样性
- 寒门（灵石 0、悟性 8）vs 世家（灵石 500、悟性 5）：用"悟性/天赋"补偿经济差，避免纯强弱。
- 稀根（雷 / 冰 / 混沌）：高资质但低爆率，作为"赌运"选项，不破坏平衡（仅为体验差异）。

---

## 六、战斗与风险平衡

战斗由 AI 主持（无确定性命中公式，ENEMY_POOL 仅为立绘/命名），平衡靠**三道闸**：

1. **HP 增量钳制**：单回合 `hp` 增量 **[−40, +40]**；`[致命]` 事件最多把满血打到 1，不直接归零（除非 Boss 级且剧情明确写明）。
2. **失败 ≠ 死亡**：战斗失利 → 重伤（HP ×0.4）+ 失物 / 失灵石，而非必死；只有 `[致命]` 且 HP 已低、或 Boss 战才真死。降低挫败感，让"输一场"仍可玩。
3. **风险标签语义一致**：`[平安]` 几乎无伤、`[凶险]` 可控伤、`[致命]` 高伤/可能死——标签须与 delta 量级一致（在 AI 提示词里强化）。

敌人强度梯度：按 `location.danger`（0~10）与 `realm_suggest` 给 AI 参照，避免低境遇到秒杀级 Boss。

---

## 七、元循环节奏与危机曲线

当前：`chance = 0.022 + (turn − 12) × 0.0014`，仅 `realmLevel < cap` 才湮灭。
问题：低灵力界（cap=4）给玩家约 50~80 程窗口，配合上面的进度钳制基本能逃；但**高灵力界几乎零压力**（cap=10 时永不满足湮灭条件），失去"终局压迫"。

调优建议（与突破节奏对齐）：
- 前 12 程：免疫（新手保护）。
- 12~40 程：`2% ~ 6%`，温和。
- 40~80 程：`6% ~ 12%`。
- 80+ 程：`12% ~ 20%`，制造"必须飞升离界或重投"的终局压迫。
- 关键反转：**当 `realmLevel ≥ cap` 时不再湮灭**，改为"此界稳固，可安心飞升 / 自由续写"——把 cap 从"天花板焦虑"变成"安全线"。

### 本地"因果力 / 修道时长"（排行榜占位）
- **因果力** = Σ(每程 token 估算) 或 Σ(抉择数 × 风险权重)，风险越高权重越大（呼应你"对话越多→令牌越多→因果力越高"的设想）。
- **修道时长** = 总回合 / 11（换算"年"）或总天数。
- 现在仅本地神魂册记录，待 Path B 后端做跨玩家排行。

---

## 八、需要改代码的平衡项（清单）

| 优先级 | 项 | 改法 |
|---|---|---|
| P0 | 突破成功率倒置 | 改用"基础易度随境递减"公式（见四） |
| P0 | realm_progress 无钳制 | `processAction` 对 `realm_progress` delta `clamp[−5,+12]` |
| P1 | hp / 灵力 delta 无上限 | 单回合 `hp clamp[−40,+40]`，`qi` 不超 `maxQi` |
| P1 | 战斗失利=死亡过狠 | 失败 → 重伤/失物，非必死 |
| P1 | 危机曲线平淡 | 分段概率 + `realmLevel≥cap` 转安全线 |
| P2 | 灵石无收入锚 | 在 AI 提示词固定"任务/探索"收入区间 |
| P2 | 正邪无机制 | 高 evil 解锁魔道技但锁正道宗门，反之亦然 |

---

## 九、数值速查表

- 境界：10 大境（炼气 L1 → 飞升 L10），`maxQi` 100 → 200000（飞升期建议封顶 200000，不为 Infinity）。
- 突破率（建议）：0.85 / 0.72 / 0.60 / 0.50 / 0.42 / 0.35 / 0.28 / 0.22 / 0.15。
- 进度节奏：每界 25~60 程摸顶（受钳制后）。
- 危机概率：12 程前 0；12~40 程 2~6%；40~80 程 6~12%；80+ 程 12~20%；`realmLevel≥cap` 归零。
- 经济锚：破障丹 500 为冲关核心消费；九转还魂丹 100000 为稀有保命。
- 元循环计分：因果力（风险加权抉择数）、修道时长（总回合/11）。

---

*注：本设计为策划方案，未改动任何游戏代码。标注 P0 的两项（突破率倒置 + 进度钳制）建议优先落地，可立即消除当前最破坏体验的两个问题。*
