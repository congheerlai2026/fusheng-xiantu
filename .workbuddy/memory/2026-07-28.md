# 2026-07-28 工作日志

## 《浮生仙途》功能实现（全部完成）
- 实现 7 项功能并通过 node --check：双飞升目标 HUD（苦修·问道/以技证道）、短局模式 quick(18回合收束)、一键分享卡 shareCard(canvas PNG)、区域感官简报 buildLocationBrief 注入提示词、地点→场景自动映射(插图增强)、开局强钩子抉择、data.js 全 10 LOCATIONS 补 scene+sensory。
- 编辑文件：data.js / ai.js / game.js / index.html / style.css。

## 部署准备
- 已同步 根目录 → `dist/` 与 `deploy-public/`（含 assets/ 17张webp、fonts/Zpix.ttf）。
- `deploy-public/` 内 git 已 commit：`3253f60`（main，7文件变更，仓库 12MB，无 remote）。
- 本地 git 身份暂用 local：fusheng-bot / bot@workbuddy.local（待用户确认其 GitHub 身份，可 amend）。
- 本地预览服务器已起：`http://127.0.0.1:8123`（python http.server 指向 dist/），新功能已验证在线。

## 部署进展（已找到仓库）
- 用户在另一个任务已建好 GitHub 仓库，并已在 `deploy-public/` 配好 remote：
  `origin = https://github.com/congheerlai2026/fusheng-xiantu.git`（公开仓库）。
- 远程 main 位于 `9206782`（与本地 base 同一 hash）；本地 main 仅领先 1 个提交 `3253f60`（新功能），无分叉，可 fast-forward 干净推送。
- 推送仍需凭据：已用 `GIT_TERMINAL_PROMPT=0 git push` 验证，报 `could not read Username`（无 token / credential helper / git-credentials 文件 / SSH key）。
- **已用用户提供的 PAT 完成部署**：① `git commit --amend` 将 3253f60 作者对齐为 `WorkBuddy <deploy@local>`（新 hash `6d34420`）；② `git push` main 成功（`9206782..6d34420` fast-forward）；③ 查得 Pages 早已启用（另一任务开的），`status=built, public=true, source=main/`，无需再开。
- **公网链接（已验证 200 + 新功能在线 + 无 key 泄露）**：`https://congheerlai2026.github.io/fusheng-xiantu/`
- 本地 `dist/` 与 `deploy-public/` 内容一致（均含新功能 + assets/fonts）。
- 提醒用户：PAT 用完应去 GitHub revoke；微信仍无法代发，用公网链接 + 游戏内分享卡替代。
2. **发微信链接**：当前工具环境无任何“发微信消息”能力（仅有微信支付/绑卡类技能）。替代方案：上线 GitHub Pages 后交付公网 URL + 游戏内“生成仙途分享卡”PNG，由用户自行转发微信。

## 环境注意
- git -C 对中文路径失败，须用 `cd` 子shell 执行 git 命令。
- git 报 dubious ownership，已 `git config --global --add safe.directory` 加例外。
- 用户 DeepSeek key 已用于游戏（deepseek-v4-flash）。

## 叙事优化（"全AI但差点意思" → 直接优化并部署）
- 跑 4 遍真实生成（gen_test.cjs，VM 沙箱载入真实 ai.js/data.js 调 DeepSeek），定位 5 类"差点意思"：
  ① 告诉不展示（心中念头急转/出尘之气/五感稍敏）② 陈词滥调（落日熔金/出尘之气/敬酒不吃吃罚酒）
  ③ 复读 location brief 原句（S1 复读"柴烟与艾草"、S4 逐字"丹香、铁锈与脂粉味"）
  ④ 选项风险平淡（S4 四选项全[平安]）⑤ 无辨识度文风。
- 根因 Bug：deepseek-v4-flash 是推理模型，reasoning_tokens 计入 max_tokens 挤占 narrative，standard 仅 1500 上限致截断/脆弱。
- 优化（ai.js）：① 新增【叙事工艺】强指令块（展示不告诉/感官锚定≥3感/反陈词滥调/选项须有真实差异+代价+至少1个[凶险]/至少1个[平安]/短句留白/情绪钩子收尾/辨识度文风）；② buildLocationBrief 加"禁逐字抄 prompt 原句"；③ buildLifeSkillGraph 输出 LIFE_SKILLS.flavor（匠心与代价）；④ 修复 getMaxTokens：新增 `_isReasoningModel`，推理模型 standard 上限 1500→2600、immersive 2000→3400（Math.max(desired,cap) 保证 options 不被截断）。
- 优化（data.js）：① 10 LOCATIONS.sensory 改写为多感官+钩子的层次化描写；② LIFE_SKILLS 每项加 flavor（炼丹守炉灼指、御兽血契损寿等匠心代价 vignette）；③ REALMS.desc 加破境体感。
- 复跑验证（outputs/samples_v2/）：4 场景均无陈词滥调/无逐字复读、show-don't-tell、每场景≥1[凶险]+≥1[平安]；S3 炼丹 flavor 自然呈现、S4 已修掉全[平安]。
- 部署：同步 root→dist/ 与 deploy-public/；commit `c585f2f`；push `6d34420..c585f2f`；已验证 GitHub Pages 公网 `ai.js`/`data.js` 含新内容（叙事工艺+reasoning fix+flavor）。
- 评测脚本 gen_test.cjs 已改用 AIService.getMaxTokens 计算 max_tokens 并输出 usage。
